# .env.example — copy to .env.local

# PostgreSQL — Neon free tier: https://neon.tech
DATABASE_URL="postgresql://user:password@ep-xxx.aws.neon.tech/neondb?sslmode=require"

# Protects the POST /api/refresh endpoint (pick any string)
REFRESH_SECRET="rallyiq2026"

# No API key needed for match schedule — uses ESPN's free public API
# Optional future: The Odds API for real market odds (free tier 500 req/month)
# THE_ODDS_API_KEY=""
